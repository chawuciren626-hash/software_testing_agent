# 测试用例（由需求生成）
- 来源：extensions/requirements_to_cases/sample_requirements.md
- 生成时间：2026-09-09 16:52
- 用例数：15

| id | 标题 | 模块 | 类型 | 优先级 | 前置 | 步骤 | 预期 | 可自动化 |
|---|---|---|---|---|---|---|---|---|
| REQ-001-F | 管理员可以使用用户名 + 密码登录后台，登录成功后返回 token（功能） | 待定 | 功能 | P1 | 系统已部署且可访问 | 1. 按需求执行：管理员可以使用用户名 + 密码登录后台，登录成功后返回 token | 功能按预期正常完成，无报错 | 可（pytest/Playwright） |
| REQ-001-B | 管理员可以使用用户名 + 密码登录后台，登录成功后返回 token（边界） | 待定 | 边界 | P2 | 准备边界输入数据 | 1. 使用边界值（最小/最大/临界）执行：管理员可以使用用户名 + 密码登录后台，登录成功后返回 token | 边界条件下系统处理正确，无越界/崩溃 | 可（pytest/Playwright） |
| REQ-001-N | 管理员可以使用用户名 + 密码登录后台，登录成功后返回 token（异常） | 待定 | 异常 | P1 | 准备异常/非法输入 | 1. 输入非法/缺失数据执行：管理员可以使用用户名 + 密码登录后台，登录成功后返回 token | 系统给出明确错误提示，不发生脏数据/异常中断 | 可（pytest/Playwright） |
| REQ-002-F | 管理员可以注册新账号，用户名全局唯一（功能） | 待定 | 功能 | P1 | 系统已部署且可访问 | 1. 按需求执行：管理员可以注册新账号，用户名全局唯一 | 功能按预期正常完成，无报错 | 可（pytest/Playwright） |
| REQ-002-B | 管理员可以注册新账号，用户名全局唯一（边界） | 待定 | 边界 | P2 | 准备边界输入数据 | 1. 使用边界值（最小/最大/临界）执行：管理员可以注册新账号，用户名全局唯一 | 边界条件下系统处理正确，无越界/崩溃 | 可（pytest/Playwright） |
| REQ-002-N | 管理员可以注册新账号，用户名全局唯一（异常） | 待定 | 异常 | P1 | 准备异常/非法输入 | 1. 输入非法/缺失数据执行：管理员可以注册新账号，用户名全局唯一 | 系统给出明确错误提示，不发生脏数据/异常中断 | 可（pytest/Playwright） |
| REQ-003-F | 登录失败（密码错误）时应提示明确错误且不返回 token（功能） | 待定 | 功能 | P1 | 系统已部署且可访问 | 1. 按需求执行：登录失败（密码错误）时应提示明确错误且不返回 token | 功能按预期正常完成，无报错 | 可（pytest/Playwright） |
| REQ-003-B | 登录失败（密码错误）时应提示明确错误且不返回 token（边界） | 待定 | 边界 | P2 | 准备边界输入数据 | 1. 使用边界值（最小/最大/临界）执行：登录失败（密码错误）时应提示明确错误且不返回 token | 边界条件下系统处理正确，无越界/崩溃 | 可（pytest/Playwright） |
| REQ-003-N | 登录失败（密码错误）时应提示明确错误且不返回 token（异常） | 待定 | 异常 | P1 | 准备异常/非法输入 | 1. 输入非法/缺失数据执行：登录失败（密码错误）时应提示明确错误且不返回 token | 系统给出明确错误提示，不发生脏数据/异常中断 | 可（pytest/Playwright） |
| REQ-004-F | 注册时用户名缺失应被拒绝并提示必填（功能） | 待定 | 功能 | P1 | 系统已部署且可访问 | 1. 按需求执行：注册时用户名缺失应被拒绝并提示必填 | 功能按预期正常完成，无报错 | 可（pytest/Playwright） |
| REQ-004-B | 注册时用户名缺失应被拒绝并提示必填（边界） | 待定 | 边界 | P2 | 准备边界输入数据 | 1. 使用边界值（最小/最大/临界）执行：注册时用户名缺失应被拒绝并提示必填 | 边界条件下系统处理正确，无越界/崩溃 | 可（pytest/Playwright） |
| REQ-004-N | 注册时用户名缺失应被拒绝并提示必填（异常） | 待定 | 异常 | P1 | 准备异常/非法输入 | 1. 输入非法/缺失数据执行：注册时用户名缺失应被拒绝并提示必填 | 系统给出明确错误提示，不发生脏数据/异常中断 | 可（pytest/Playwright） |
| REQ-005-F | 连续 5 次登录失败，账号锁定 10 分钟（功能） | 待定 | 功能 | P1 | 系统已部署且可访问 | 1. 按需求执行：连续 5 次登录失败，账号锁定 10 分钟 | 功能按预期正常完成，无报错 | 可（pytest/Playwright） |
| REQ-005-B | 连续 5 次登录失败，账号锁定 10 分钟（边界） | 待定 | 边界 | P2 | 准备边界输入数据 | 1. 使用边界值（最小/最大/临界）执行：连续 5 次登录失败，账号锁定 10 分钟 | 边界条件下系统处理正确，无越界/崩溃 | 可（pytest/Playwright） |
| REQ-005-N | 连续 5 次登录失败，账号锁定 10 分钟（异常） | 待定 | 异常 | P1 | 准备异常/非法输入 | 1. 输入非法/缺失数据执行：连续 5 次登录失败，账号锁定 10 分钟 | 系统给出明确错误提示，不发生脏数据/异常中断 | 可（pytest/Playwright） |
